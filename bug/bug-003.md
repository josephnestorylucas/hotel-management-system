# Bug #3 — Frontdesk User Failing to Log Out

**Date:** 2026-05-29
**Status:** Not Reproducible
**Page URL:** http://hms.dadyprojects.systems/profile

---

## Reported Issue

Frontdesk user unable to log out from the profile page.

## Investigation

Checked:
- `POST /logout` route in `routes/web.php:132`
- `LoginController::logout()` in `app/Http/Controllers/Auth/LoginController.php:51`
- Logout form in `resources/views/layouts/app.blade.php:56` (has `@csrf`, correct `POST` method)
- Session config in `.env` (database driver, secure cookie, same-site strict)
- Frontdesk sidebar (`resources/views/shared/sidebar/front-desk.blade.php`)
- `SecurityHeaders` middleware — no blocking CSP rules
- `RoleMiddleware` — logout route has no role restriction

## Verdict

**Bug not reproducible on client.** Logout works correctly — form, route, and controller are all properly wired.
